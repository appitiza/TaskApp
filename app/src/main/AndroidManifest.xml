<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.appitiza.task">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".ui.post.PostActivity"/>
        <activity android:name=".ui.store.StoreActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeStore"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.detailed.DetailedActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeDetailed"/>
    </application>

</manifest>